%div{:dir => "rtl"}
  %h1 כל המטפלים
.CSSTableGenerator
  %table
    %tr
      %td שם משתמש
      %td שם מלא 
      %td
      %td
      %td
      
    - @users.each do |user|
      - if user.admin != true
        %tr
          %td= user.userName
          %td= user.fullName
          %td= link_to 'הצג', user
          %td= link_to 'עריכה', edit_user_path(user)
          %td= link_to 'מחיקה', user, :method => :delete, :data => { :confirm => 'האם אתה בטוח?' }

%br
%div{:dir => "rtl"}
  %button.save{ :onclick => "window.location.href='/users/new'"} מטפל חדש
